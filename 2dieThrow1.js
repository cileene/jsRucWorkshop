/*
function dieThrow1() {
    fill("white");
    textSize(20);
    text(dieText[0], windowWidth/4, windowHeight/2);
      
    if(mousePressed) {
      // !!! TILFØJ TOUCH !!!
      dieText.shift();
      if (dieText.length === 0) {
        // test at det virker
        console.log("intro færdig");
        // go on to dieThrow1
        dieThrow1();
      }
    }

        
}
*/